#include <iostream>

using namespace std;

int main()
{
    int n, sum = 0, cnt = 0;
    cin >> n;

    for (int i = 0; i < n; i++)
    {
        cnt++;
    }
    cout << cnt << endl
         << 1;
}